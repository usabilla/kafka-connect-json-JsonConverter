plugins {
    id 'java'
}

group 'com.usabilla.kafka.connect.json'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'

    compile 'org.apache.kafka:connect-json:2.1.0'
}
